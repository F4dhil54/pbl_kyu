import 'package:flutter/material.dart';

class AppColors {
  // Main colors
  static const Color primary = Color(0xFF3333CC); // A vibrant deep blue like in the design
  static const Color background = Color(0xFFF6F6F6); // Light off-white grey background
  static const Color surface = Colors.white;
  
  // Text colors
  static const Color textMain = Color(0xFF111111);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textLight = Colors.white;
  
  // Status colors
  static const Color success = Color(0xFF2E7D32); // For "Completed" / "Active"
  static const Color pending = Color(0xFFEBB4F6); // Light purple/pink for pending pill
  static const Color pendingText = Color(0xFF8E24AA); // Darker purple text for pending pill
  
  // Inputs & borders
  static const Color inputBackground = Color(0xFFEAEAEA); // Grey for text fields
  static const Color border = Color(0xFFE0E0E0);
}
